First, create all files that you find below the `root` directory structure.

When you clone the repository, you do not have to create the files in the `scripts` folder. You can start the scripts from the repository folder.

When you want to start "shell script" files, make sure that you can start them. Use `chmod +x` for this.
